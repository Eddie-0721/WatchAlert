fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
# Pin the Agents SDK API surface used by the OpenAI-compatible Chat
# Completions model adapter. This avoids an unreviewed major upgrade changing
# Tool Calling or streaming behaviour in production.
openai-agents>=0.21,<0.22
